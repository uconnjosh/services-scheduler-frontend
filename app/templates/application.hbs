<div class="top-nav">

	<div>
		{{!-- <LinkTo>Signup</LinkTo> --}}

		{{#if session.authenticated}}
			<p>Hi! {{session.authenticated.email}}</p>
		{{else}}
			<LinkTo @route="login">Login</LinkTo>
		{{/if}}
	</div>

</div>

{{outlet}}