<div class="flex">

	<div class="w-70">

		<h1>
			{{t "services_type"}}	
		</h1>

		<ul>
			{{#each model as |service|}}
				<li>
					{{service.title}} | {{service.duration}}
				</li>
			{{/each}}
		</ul>

	</div>

	<div class="w-30">
		{{outlet}}
	</div>

</div>

