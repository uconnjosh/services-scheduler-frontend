<div>

	<h1>
		{{model.title}}
	</h1>

	<p>
		{{model.description}}
	</p>

	<div class="flex">
		{{#each model.images as |image|}}
			<img
				src="{{image.src}}"
				class="w-30"
			/>
		{{/each}}
	</div>

	{{#each model.occurrences as |occurrence|}}
		<div>
			{{occurrence.slots}} spaces open @ {{moment-calendar occurrence.starts}}
		</div>
	{{/each}}

</div>

